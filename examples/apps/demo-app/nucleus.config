version = "1.0.0"

[server]
port = 3000
host = "0.0.0.0"
environment = "${APP_ENV|development}"

[database]
url = "${DATABASE_URL|sqlite:demo.db}"

[payments]
stripe_key = "${STRIPE_KEY|sk_test_mock}"
