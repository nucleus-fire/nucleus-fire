# Render Blueprint
# Generated by: nucleus deploy
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: site
    env: docker
    plan: free
    region: ohio
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
